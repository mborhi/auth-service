describe("Login", () => {

    it("correctly stores given token with session id", () => {

    });

    it("correctly makes a request to the Spotify Web API token endpoint", () => {

    });

    it("correctly handles a token response from the Spotify Web API", () => {

    });

    it("correctly handles any errors from the Spotify Web API", () => {

    });

});