describe("Token", () => {

    it("correctly retrieves token from Redis cache", () => {

    });

    it("correctly makes a request to the Spotify Web API if the token is expired", () => {

    });

    it("correctly updates the cache with a new token", () => {

    });

});

// TODO: add LFU for cache